<%= form_with(model: admins_word) do |form| %>
  <% if admins_word.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admins_word.errors.count, "error") %> prohibited this admins_word from being saved:</h2>

      <ul>
        <% admins_word.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
